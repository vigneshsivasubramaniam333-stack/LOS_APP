FROM maven:3.9-eclipse-temurin-17

WORKDIR /app

# Copy full project (multi-module)
COPY . .

# Move into core service
WORKDIR /app/services/los-core-service

# Run using Maven (no jar needed)
CMD ["mvn", "spring-boot:run"]