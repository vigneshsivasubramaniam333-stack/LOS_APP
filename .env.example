# Infrastructure
POSTGRES_USER=los_admin
POSTGRES_PASSWORD=los_secret_2026
POSTGRES_HOST=localhost
POSTGRES_PORT=5432

REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=los_redis_2026

RABBITMQ_HOST=localhost
RABBITMQ_PORT=5672
RABBITMQ_USER=los_rabbit
RABBITMQ_PASSWORD=los_rabbit_2026

S3_ENDPOINT=http://127.0.0.1:9000
S3_ACCESS_KEY=los_minio_admin
S3_SECRET_KEY=los_minio_secret_2026
S3_BUCKET=los-documents
MINIO_ENDPOINT=http://127.0.0.1:9000
MINIO_ACCESS_KEY=los_minio_admin
MINIO_SECRET_KEY=los_minio_secret_2026

LOS_DOCUMENT_STORAGE=local
FILE_UPLOAD_DIR=./uploads

# JWT
JWT_SECRET=change-this-to-a-secure-random-value-in-production
JWT_ACCESS_TOKEN_EXPIRY=900000
JWT_REFRESH_TOKEN_EXPIRY=604800000

# Services
EUREKA_HOST=localhost
EUREKA_PORT=8761

# Docker prod UI (host port; keep 8080 when host nginx uses port 80)
LOS_UI_HOST_PORT=8080

# UAT/sandbox: enables Reset demo data in admin UI (local profile enables this automatically)
LOS_DEMO_ENABLED=true

# Encore LMS (remote HTTP API — no local vendor Docker stack)
# Credinnov EC2 (must include /credinnov-encore-server/ context path on :8091)
ENCORE_BASE_URL=http://credinnov-sandbox.senseitech.com/credinnov-encore-server/
ENCORE_API_USERNAME=admin
ENCORE_API_PASSWORD=password1

# PayU (same test merchant as PLP lending-service)
PAYU_MERCHANT_KEY=g8yc8J
PAYU_MERCHANT_SALT=BBc2U41mOZPLuteAYBW9wJ2Lnm9V9rQV
PAYU_GATEWAY_URL=https://test.payu.in/_payment
# Public base for PayU surl/furl (nginx: /los/api/ → los-core). Callback path appends /api/v1/webhooks/...
LOS_PUBLIC_API_BASE_URL=http://credinnov-sandbox.senseitech.com/los
LOS_BORROWER_UI_URL=http://credinnov-sandbox.senseitech.com/los/borrower
